apiVersion: maas.opendatahub.io/v1alpha1
kind: ExternalModel
metadata:
  name: granite-rhai
  namespace: llm
  annotations:
    # Path only — no scheme/host, no trailing slash.
    # Consumed by Compact MaaS BFF for test-connection + HTTPRoute URLRewrite heal.
    compact-maas/upstream-path-prefix: "/v1/projects/REPLACE-PROJECT-UUID/inference"
spec:
  provider: openai
  targetModel: REPLACE-TARGET-MODEL-ID
  endpoint: REPLACE-FQDN
  credentialRef:
    name: granite-rhai-credentials
